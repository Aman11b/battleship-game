import "./style.css"; // Import the CSS for styling
import startGame from "./dom"; // Import DOM interaction methods

// Initial DOM rendering for boards (initial empty boards)
document.addEventListener("DOMContentLoaded", () => {
  startGame(); // Start the game when the page is ready
});
